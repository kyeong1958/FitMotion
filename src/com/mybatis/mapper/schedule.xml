<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.ScheduleMapper">
	<select id="test4">
		SELECT 1 FROM dual
	</select>
<!-- ======================================= [[ 경애  ]] ====================================== -->
<!-- 예약관리에서 콤보박스에 해당하는 값 가져오기 -->
	<resultMap id="sm_map" type="hashmap"/>
	<select id="scheduleModal" parameterType="map" statementType="CALLABLE">
		{call proc_schedulemodal(#{staffList, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=sm_map}
				       , #{places, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=sm_map}
		)}
	</select>
<!-- ======================================= [[ 경애  ]] ====================================== -->
	
</mapper> 
 