<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.OwnerMapper">
	<select id="test2">
		SELECT 1 FROM dual
	</select>
	
	<select id="allsales" parameterType="map" resultType="map">
		select ms.ms_category as 목록 , sum(ms.ms_price) as 매출합계
		from payment pm ,member mb, membership ms
		where pm.mem_id = mb.mem_id and pm.ms_code = ms.ms_code
		group by ms_category
	</select>
	
	
</mapper> 
 
